<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.bookadmin.dao.BookStateDao">
	<select id="getBookState" parameterType="String" resultType="int">
		select bookState from book where scene = #{scene}
	</select>
</mapper>